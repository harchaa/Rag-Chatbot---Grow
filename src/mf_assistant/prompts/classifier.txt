Classify the user's mutual-fund question into exactly ONE label:

- FACTUAL: asks for an objective, verifiable fact about a scheme — e.g. expense ratio,
  exit load, minimum SIP/lumpsum, ELSS lock-in, benchmark index, riskometer category,
  fund manager, AUM, or how to download a statement / capital-gains report.
- ADVISORY: asks for an opinion, recommendation, suitability, prediction, or a
  comparison — e.g. "should I invest", "which is better", "will it grow",
  "is this a good fund", "fund A vs fund B".
- PERFORMANCE: asks about returns, gains, NAV history, CAGR, XIRR, past performance,
  or how a fund performed over any time period — e.g. "what returns did X give",
  "1 year return of X", "how has X performed", "CAGR of X".
- OUT_OF_SCOPE: not about mutual fund facts at all.

Respond with ONLY the label word: FACTUAL, ADVISORY, PERFORMANCE, or OUT_OF_SCOPE.

QUESTION: {question}
