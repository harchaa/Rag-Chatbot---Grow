# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Environments / secrets
.env
.venv/
venv/

# Vector index (build artifact — rebuilt from data/processed/)
.chroma/

# Raw scrape snapshots: large + volatile (daily NAV churn). Local cache only;
# data/processed/ is the versioned, human-readable audit trail.
data/raw/*
!data/raw/.gitkeep

# Caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.cache/

# Model / HF caches (if downloaded locally)
hf_cache/

# OS / editors
.DS_Store
.idea/
.vscode/
